require 'spec_helper'

describe Centras do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Centras.create!(@valid_attributes)
  end
end
